import 'package:flutter/material.dart';

class AppConstants {
  static const appName = "Find Tutor";

  static const primaryColor = Color(0xff0a305f);
  static const profileRadius = 75.0;
  static const chatProfileRadius = 28.0;
  static const headerProfileRadius = 20.0;
}
